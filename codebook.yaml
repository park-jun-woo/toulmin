required:
  feature:
    engine: "defeats graph construction and h-Categoriser verdict computation"
    rule: "rule registration, strength classification, defeats relation"
    claim: "claim type definition and claim-rule matching"
    ground: "ground type definition"
    cli: "cobra command entrypoint"
    graph: "defeats graph validation"
    analyzer: "Go AST analysis for Graph defeat extraction"
    codegen: "Go code generation for rule registration"
    policy: "policy judgment framework (authentication, authorization, IP blocking)"
    state: "state transition framework (Machine, transition guards)"
    approve: "approval workflow framework (multi-step, Flow)"
    price: "price judgment framework (discounts, coupons, promotions)"
    feature: "feature flag framework (rollout, toggle, percentage)"
    moderate: "content moderation framework (hate speech, spam, NSFW)"
  type:
    command: "cobra command entrypoint"
    model: "domain struct (Claim, Ground, Rule, Node, Graph)"
    engine: "core evaluation logic"
    parser: "annotation or input parsing"
    analyzer: "Go AST analyzer"
    validator: "defeats graph validator"
    codegen: "code generator"
    util: "shared utility"
    rule: "individual judgment rule function"
    interface: "Go interface definition"
    adapter: "HTTP middleware adapter (net/http)"

optional:
  pattern:
    error-collection: "collect errors for batch reporting"
    recursive: "recursive graph traversal"
